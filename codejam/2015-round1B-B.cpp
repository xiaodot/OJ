#include <iostream>
#include <vector>
#include <stdio.h>
#include <algorithm>
#include <sstream>
#include <string>

using namespace std;


int main(){
    freopen("in.txt", "r", stdin);
//    freopen("out.txt", "w", stdout);

    int T, test;
    cin >> T;
    for(test=1; test<=T; test++){
        int R, C, N;
        cin >> R >> C >> N;


        printf("Case #%d: %d\n", test, res);
    }

    return 0;
}

